export interface Guitar {
  id?: string;
  make: string;
  model: string;
  subModel: string;
  imageUrl: string;
  description: string;
  color: string;
  price: number;
  addDate?: Date;
}
